import React from 'react'
import {edit, trash} from '../icons'

export default function IconButton(props){
  const {type} = props

  return (
    <div className='icon--button'>
      {/* write img el for svg */}
    </div>
  )
}